print	
'	quote line of text
"	quote line of text
"""	quotes for multiple lines of text
"""<pydoc comment>"""
#	comments a line
_	underscore, acceptable in naming variables
,	
=	equals
+	addition
-	subtraction
*	multiply
/	divide
%	modulus - args are divided, result is the remainder
(	open parenthesis
)	close parenthesis
!=	not equal
>	boolean operator greater than
<	boolean operator less than
>=	boolean operator greater than or equal to
<=	boolean operator less than or equal to
true	boolean
false	boolean
%d
%s
%r
\	escape character
	\\	Backslash ()
	\'	Single-quote (')
	\"	Double-quote (")
	\a	ASCII bell (BEL)
	\b	ASCII backspace (BS)
	\f	ASCII formfeed (FF)
	\n	ASCII linefeed (LF)
	\r	ASCII Carriage Return (CR)
	\t	ASCII Horizontal Tab (TAB)
	\v	ASCII vertical tab (VT)
	\N	Character named name in the Unicode database (Unicode only)
for
while
raw_input()
from
import
import *
sys	module
	argv	container for 
		script
file()
	<variable>.open()
	<variable>.read()
	<variable>.seek()
	<variable>.write()
	<variable>.close()
	<variable>.truncate()
	w
	w+
	a
def <function>(<variable> + n1, <variable> * n2, ...):
*<variable>
return
<file or module>.function(variable)
                ^inside file/module there's a function, run it with variable
split()
pop()
+=	increment by
-=	decrement by
if
elif
else
